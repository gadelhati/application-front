X		redirect to before signin
X		unique element has already been registered
DO		filter user by admin domain / view admin / filter by OM
DO		refresh token
		expose response error without access
		add name to person/user/operator/funcion by person
		inform and close after create/update/delete
		question and inform before create/update/delete

improvements:
	
		add pagination on front
		ignoreCaseAndOrderByAscOrDesc
		use ErrorBoundary
		sidebar: hamburger
		sidebar: with tooltip wheen show icons
		sidebar: without tooltip when show title
		responsive site
		add footer
		useQuery
		endpoint
		improve @PreAuthorize
		swagger view (whitelabel error page)	
		generics:
			T.state.incial.tsx
			T.state.tsx
				redux object: { loading, error, item, itens[] }
			T.reducer.tsx
